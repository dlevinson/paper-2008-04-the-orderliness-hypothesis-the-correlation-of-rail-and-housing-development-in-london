# Shared Data Pointers

The broad London population, station-density, network, and GIS source package is staged once with the `paper-2008-03` London Coevolution package.

This Orderliness package keeps only the paper-specific correlation and supporting workbooks. It does not duplicate `PopulationAndDensity.xls` or the finalized London GIS/network package.
